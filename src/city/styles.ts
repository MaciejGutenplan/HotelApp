import { BLACK, LIGHT_BLUE, LIGHT_RED } from "Constants/colors";

export const smallPhoneDetailsPanelStyle = {
    background: LIGHT_RED,
    color: BLACK,
    fontSize: '10px'
}

export const browserDetailPanelStyle = {
    background: LIGHT_BLUE,
    color: BLACK
}